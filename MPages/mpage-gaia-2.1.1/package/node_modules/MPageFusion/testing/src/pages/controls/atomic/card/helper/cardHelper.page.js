"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
var getElement = exports.getElement = function getElement(root, contentSelector) {
    return root.getNestedElement(contentSelector);
};
var makeListHelper = exports.makeListHelper = function makeListHelper(list, itemPage, selectorList, containerSelector) {
    return {
        list: list,
        itemPage: itemPage,
        selectorList: selectorList,
        containerSelector: containerSelector
    };
};
var displayHelper = exports.displayHelper = function displayHelper(root) {
    return root.getRootElement().then(function (element) {
        return element.getText();
    });
};
var isIncludedClass = exports.isIncludedClass = function isIncludedClass(root, item) {
    return root.getRootElement().then(function (element) {
        return element.getAttribute("class");
    }).then(function (classNames) {
        return !!classNames.includes(item);
    });
};