"use strict";

import SvgIcon from "../../SvgIcon";
import { cancel } from "../../svgDefinitions";

/**
 * The cancel icon class constructor
 * @class Cancel
 * @extends SvgIcon
 */
export default class Cancel extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Cancel";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return cancel;
    }
}
