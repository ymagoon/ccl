"use strict";

import SvgIcon from "../../SvgIcon";
import { required } from "../../svgDefinitions";

export default class Required extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Required";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return required;
    }
}
