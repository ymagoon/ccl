
const NAMESPACE = "mpageui-ScopedSearch";

const THROTTLE_TIME = 300;

/**
 * Child name constants for ScopedSearch.
 * @type {{SELECT: string, SEARCH: string}}
 */
const CHILDREN = {
    SELECT: "filterSelect",
    SEARCH: "filterSearch"
};

/**
 * Public events for the ScopedSearch.
 * @type {{SEARCH_INPUT: string, SEARCH_SUBMIT:string, SEARCH_SELECTION_CHANGE:string}}
 */
const EVENTS = {
    SEARCH_INPUT: "ScopedSearch::Search::input",
    SEARCH_SUBMIT: "ScopedSearch::Search::submit",
    SEARCH_SELECTION_CHANGE: "ScopedSearch::Search::selectionChange"
};


export {
    CHILDREN,
    EVENTS,
    NAMESPACE,
    THROTTLE_TIME
};
