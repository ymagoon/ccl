"use strict";

import SvgIcon from "../../SvgIcon";
import { swap } from "../../svgDefinitions";

/**
 * The Swap icon class constructor
 * @class Swap
 * @extends SvgIcon
 */
export default class Swap extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Swap";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return swap;
    }
}
