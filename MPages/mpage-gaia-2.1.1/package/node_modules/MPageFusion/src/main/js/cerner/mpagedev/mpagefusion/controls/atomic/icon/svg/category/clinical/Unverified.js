"use strict";

import SvgIcon from "../../SvgIcon";
import { diamond } from "../../svgDefinitions";

/**
 * The Unverified icon class constructor
 * @class Add
 * @extends SvgIcon
 */
export default class Unverified extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Unverified";
    }

    /**
     * @inheritDoc
     */
    initialProps() {
        const props = super.initialProps();
        return Object.assign(props, {
            defaultClassNames: `${props.defaultClassNames} mpageui-SvgIcon-clinical-unverified`
        });
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return diamond;
    }
}
