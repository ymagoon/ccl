/**
 * @fileoverview
 * @author Gyandeep Singh
 */

"use strict";

import SvgIcon from "../../SvgIcon";
import { infoI } from "../../SvgIconDefinitions";
import { colors } from "../../helpers/const";

export default class Info extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Info";
    }

    /**
     * @inheritDoc
     */
    getRenderedBody(el) {
        return [
            el("circle", {
                "data-svg-id": "background-circle",
                cx: "24",
                cy: "24",
                r: "24",
                fill: colors.info
            }, []),
            el("path", {
                "data-svg-id": "i-body",
                d: infoI,
                fill: colors.white
            }, []),
            el("circle", {
                "data-svg-id": "i-dot",
                fill: colors.white,
                cx: "26.5",
                cy: "11.5",
                r: "3.5"
            }, [])
        ];
    }
}
