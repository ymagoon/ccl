"use strict";

import SvgIcon from "../../SvgIcon";
import { implant } from "../../svgDefinitions";

/**
 * The Implant  icon class constructor
 * @class Implant
 * @extends SvgIcon
 */
export default class Implant extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Implant";
    }

    /**
     * @inheritDoc
     */
    initialProps() {
        const props = super.initialProps();
        return Object.assign(props, {
            defaultClassNames: `${props.defaultClassNames} mpageui-u-text-color-clinical-implant`
        });
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return implant;
    }
}
