"use strict";

import SvgIcon from "../../SvgIcon";
import { low } from "../../svgDefinitions";

/**
 * The Low icon class constructor
 * @class Low
 * @extends SvgIcon
 */
export default class Low extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Low";
    }

    /**
     * @inheritDoc
     */
    initialProps() {
        const props = super.initialProps();
        return Object.assign(props, {
            defaultClassNames: `${props.defaultClassNames} mpageui-SvgIcon-clinical-low`
        });
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return low;
    }
}
