"use strict";

import SvgIcon from "../../SvgIcon";
import { close } from "../../SvgIconDefinitions";

/**
 * The Clear icon class constructor
 * @class Clear
 * @extends SvgIcon
 */
export default class Clear extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Clear";
    }

    /**
     * @inheritDoc
     */
    initialProps() {
        const props = super.initialProps();
        return Object.assign(props, {
            path: [ close ]
        });
    }
}
