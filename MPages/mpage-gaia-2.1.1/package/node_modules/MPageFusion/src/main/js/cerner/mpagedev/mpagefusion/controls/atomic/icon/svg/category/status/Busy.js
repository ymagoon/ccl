"use strict";

import SvgIcon from "../../SvgIcon";
import { busy } from "../../svgDefinitions";

/**
 * The Busy icon class constructor
 * @class Busy
 * @extends SvgIcon
 */
export default class Busy extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Busy";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return busy;
    }
}
