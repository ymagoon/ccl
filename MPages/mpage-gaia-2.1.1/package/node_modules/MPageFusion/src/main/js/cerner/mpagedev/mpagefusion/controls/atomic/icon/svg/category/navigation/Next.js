"use strict";

import SvgIcon from "../../SvgIcon";
import { next } from "../../svgDefinitions";

/**
 * The Next icon class constructor
 * @class Next
 * @extends SvgIcon
 */
export default class Next extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Next";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return next;
    }
}
