"use strict";

import SvgIcon from "../../SvgIcon";
import { calendar } from "../../svgDefinitions";

/**
 * The Lookback icon class constructor
 * @class Lookback
 * @extends SvgIcon
 */
export default class Lookback extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Lookback";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return calendar;
    }
}
