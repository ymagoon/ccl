import UIComponent from "../../../base";

export default class ListDivider extends UIComponent {
    view(el, props, children, mChildren) {
        return el("div", { class: "mpageui-ListDivider" }, []);
    }
}
