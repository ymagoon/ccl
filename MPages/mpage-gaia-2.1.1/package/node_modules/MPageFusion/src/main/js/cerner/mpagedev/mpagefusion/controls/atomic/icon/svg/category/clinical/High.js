"use strict";

import SvgIcon from "../../SvgIcon";
import { high } from "../../svgDefinitions";

/**
 * The High icon class constructor
 * @class High
 * @extends SvgIcon
 */
export default class High extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "High";
    }

    initialProps() {
        const props = super.initialProps();
        return Object.assign(props, {
            defaultClassNames: `${props.defaultClassNames} mpageui-SvgIcon-clinical-high`
        });
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return high;
    }
}
