// Use the _testRoot alias from the karma config to resolve test files
var testsContext = require.context("_specRoot", true, /\.js$/);
testsContext.keys().forEach(testsContext);

// Use the _srcRoot alias from the karma config to resolve src files
var srcContext = require.context("_srcRoot", true, /\.js$/);
srcContext.keys().forEach(srcContext);
