/**
 * @module wud/constants
 */

/**
 * @namespace constants
 * @property {Object} error
 * @property {Object} error.config - errors that come from handling the configuration
 * @property {String} error.config.getFiles - the location of source files
 * @property {String} error.config.getMarkdownOutputLocation - the location to export a markdown file
 */
const constants = {
    error: {
        config: {
            getFiles: "Config::getFiles",
            getMarkdownOutputLocation: "Config::getMarkdownOutputLocation"
        }
    }
};

module.exports = constants;
