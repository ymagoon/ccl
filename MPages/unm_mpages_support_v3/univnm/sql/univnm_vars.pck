create or replace package univnm_vars is
  -- Purpose : environment variables for univnm functinos
  
  -- Globals:
      v_trans_id     number;
      v_person_id    number;
      v_user_name    varchar2(4000);

end univnm_vars;
/
