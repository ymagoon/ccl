"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.clickModifier = void 0;

var _seleniumWebdriver = require("selenium-webdriver");

var compose = function compose() {
  for (var _len = arguments.length, fns = new Array(_len), _key = 0; _key < _len; _key++) {
    fns[_key] = arguments[_key];
  }

  return function (args) {
    return fns.reduceRight(function (accumulator, nextFn) {
      return nextFn(accumulator);
    }, args);
  };
};
/**
 * Modify a click function on an element to press keys. Useful for doing
 * special clicks in controls that have multi-select capabilities.
 *
 * @memberOf module:MPageFusion/testing.helpers.click
 * @param {WebDriver} driver - Web driver instance
 * @returns {ActionSequence} Web driver action after performing
 */


var clickModifier = function clickModifier(driver) {
  return function (opts) {
    if (opts === void 0) {
      opts = {
        shiftKey: false,
        ctrlKey: false
      };
    }

    return function (element) {
      return compose(function (action) {
        return action.perform();
      }, function (action) {
        return action.click(element);
      }, function (action) {
        return opts.ctrlKey ? action.keyDown(_seleniumWebdriver.Key.CONTROL) : action;
      }, function (action) {
        return opts.shiftKey ? action.keyDown(_seleniumWebdriver.Key.SHIFT) : action;
      })(new _seleniumWebdriver.ActionSequence(driver));
    };
  };
};

exports.clickModifier = clickModifier;