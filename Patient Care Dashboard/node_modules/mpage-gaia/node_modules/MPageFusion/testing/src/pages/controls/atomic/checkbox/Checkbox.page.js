"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _seleniumWebdriver = require("selenium-webdriver");

var _UIComponent = _interopRequireDefault(require("../../../UIComponent.page"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; subClass.__proto__ = superClass; }

var ROOT_SELECTOR = ".mpageui-Checkbox";
var CHECKBOX_INPUT = ".mpageui-Checkbox-input";
var CHECKBOX_DISPLAY = ".mpageui-Checkbox-display";
/**
 * The CheckboxPage class
 * @class module:MPageFusion/testing.atomic.checkbox.CheckboxPage
 *
 * @example
 * const checkbox = new CheckboxPage(driver);
 * checkbox.get("isSelected")
 *     .then((isSelected) => {
 *         assert.isTrue(isSelected);
 *     });
 */

var CheckboxPage =
/*#__PURE__*/
function (_UIComponentPage) {
  _inheritsLoose(CheckboxPage, _UIComponentPage);

  function CheckboxPage() {
    return _UIComponentPage.apply(this, arguments) || this;
  }

  var _proto = CheckboxPage.prototype;

  /**
   * clicks the checkbox
   * @memberOf module:MPageFusion/testing.atomic.checkbox.CheckboxPage
   * @returns {Promise} resolves when the action is completed
   */
  _proto.click = function click() {
    return this.getRootElement().then(function (element) {
      return element.click();
    });
  };

  _createClass(CheckboxPage, [{
    key: "elementSelector",
    get: function get() {
      return ROOT_SELECTOR;
    }
    /**
     * the checkbox display
     * @memberOf module:MPageFusion/testing.atomic.checkbox.CheckboxPage
     * @returns {Promise<String>} display
     */

  }, {
    key: "display",
    get: function get() {
      return this.getRootElement().then(function (element) {
        return element.findElement(_seleniumWebdriver.By.css(CHECKBOX_DISPLAY));
      }).then(function (element) {
        return element.getText();
      });
    }
    /**
     * whether the checkbox is selected
     * @memberOf module:MPageFusion/testing.atomic.checkbox.CheckboxPage
     * @returns {Promise<Boolean>} isSelected
     */

  }, {
    key: "isSelected",
    get: function get() {
      return this.getRootElement().then(function (element) {
        return element.findElement(_seleniumWebdriver.By.css(CHECKBOX_INPUT)).then(function (inputElement) {
          return inputElement.getAttribute("aria-selected");
        }).then(function (attribute) {
          return Boolean(attribute);
        });
      });
    }
    /**
     * whether the checkbox is disabled
     * @memberOf module:MPageFusion/testing.atomic.checkbox.CheckboxPage
     * @returns {Promise<Boolean>} isDisabled
     */

  }, {
    key: "isDisabled",
    get: function get() {
      return this.getRootElement().then(function (element) {
        return element.findElement(_seleniumWebdriver.By.css(CHECKBOX_INPUT));
      }).then(function (inputElement) {
        return inputElement.getAttribute("disabled");
      }).then(function (attribute) {
        return Boolean(attribute);
      });
    }
  }]);

  return CheckboxPage;
}(_UIComponent["default"]);

exports["default"] = CheckboxPage;