"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _UIComponent = _interopRequireDefault(require("../../../UIComponent.page"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; subClass.__proto__ = superClass; }

var ROOT_SELECTOR = ".mpageui-Anchor";
/**
 * The AnchorPage class
 * @class module:MPageFusion/testing.atomic.label.AnchorPage
 *
 * @example
 * const anchor = new AnchorPage(driver);
 */

var AnchorPage =
/*#__PURE__*/
function (_UIComponentPage) {
  _inheritsLoose(AnchorPage, _UIComponentPage);

  function AnchorPage() {
    return _UIComponentPage.apply(this, arguments) || this;
  }

  var _proto = AnchorPage.prototype;

  /**
   * clicks the anchor
   * @memberOf module:MPageFusion/testing.atomic.label.AnchorPage
   * @returns {Promise} resolves when the action is completed
   */
  _proto.click = function click() {
    return this.getRootElement().then(function (element) {
      return element.click();
    });
  };

  _createClass(AnchorPage, [{
    key: "elementSelector",
    get: function get() {
      return ROOT_SELECTOR;
    }
    /**
     * the anchor display
     * @memberOf module:MPageFusion/testing.atomic.label.AnchorPage
     * @returns {Promise<String>} display
     */

  }, {
    key: "display",
    get: function get() {
      return this.getRootElement().then(function (element) {
        return element.getText();
      });
    }
    /**
     * whether the anchor is disabled
     * @memberOf module:MPageFusion/testing.atomic.label.AnchorPage
     * @returns {Promise<Boolean>} isDisabled
     */

  }, {
    key: "isDisabled",
    get: function get() {
      return this.getRootElement().then(function (inputElement) {
        return inputElement.getAttribute("disabled");
      }).then(function (attribute) {
        return Boolean(attribute);
      });
    }
  }]);

  return AnchorPage;
}(_UIComponent["default"]);

exports["default"] = AnchorPage;