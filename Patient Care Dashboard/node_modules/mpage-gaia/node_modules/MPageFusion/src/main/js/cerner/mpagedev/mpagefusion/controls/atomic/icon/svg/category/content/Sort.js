"use strict";

import SvgIcon from "../../SvgIcon";
import { chevronDown } from "../../svgDefinitions";

/**
 * The Sort icon class constructor
 * @class Sort
 * @extends SvgIcon
 */
export default class Sort extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Sort";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return chevronDown;
    }
}
