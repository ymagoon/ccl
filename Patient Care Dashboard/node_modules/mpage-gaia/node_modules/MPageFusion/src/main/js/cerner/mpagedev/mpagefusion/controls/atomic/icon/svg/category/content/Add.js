"use strict";

import SvgIcon from "../../SvgIcon";
import { add } from "../../svgDefinitions";

/**
 * The Add icon class constructor
 * @class Add
 * @extends SvgIcon
 */
export default class Add extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Add";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return add;
    }
}
