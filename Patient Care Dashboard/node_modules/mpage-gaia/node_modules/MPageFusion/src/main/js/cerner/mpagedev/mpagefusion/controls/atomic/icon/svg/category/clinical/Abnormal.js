"use strict";

import SvgIcon from "../../SvgIcon";
import { abnormal } from "../../svgDefinitions";

/**
 * The Abnormal icon class constructor
 * @class Abnormal
 * @extends SvgIcon
 */
export default class Abnormal extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Abnormal";
    }

    /**
     * @inheritDoc
     */
    initialProps() {
        const props = super.initialProps();
        return Object.assign(props, {
            defaultClassNames: `${props.defaultClassNames} mpageui-SvgIcon-clinical-abnormal`
        });
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return abnormal;
    }
}
