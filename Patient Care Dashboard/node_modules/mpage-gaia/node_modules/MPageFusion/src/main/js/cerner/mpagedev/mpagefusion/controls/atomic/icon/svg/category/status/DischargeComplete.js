/**
 * @fileoverview
 * @author Komala S
 */

"use strict";

import SvgIcon from "../../SvgIcon";
import { dischargeComplete } from "../../svgDefinitions";

export default class DischargeComplete extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "DischargeComplete";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return dischargeComplete;
    }
}
