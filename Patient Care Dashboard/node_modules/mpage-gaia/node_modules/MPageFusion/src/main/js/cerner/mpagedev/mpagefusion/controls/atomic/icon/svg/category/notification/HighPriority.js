"use strict";

import SvgIcon from "../../SvgIcon";
import { highPriority } from "../../svgDefinitions";

/**
 * The HighPriority icon class constructor
 * @class HighPriority
 * @extends SvgIcon
 */
export default class HighPriority extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "HighPriority";
    }

    /**
    * @inheritDoc
    */
    getSvg() {
        return highPriority;
    }
}
