"use strict";

let uniqueId = 0;

/**
 * Generates a unique prefixed identifier.
 * @param {string} prefix - The prefix for the unique identifier.
 * @returns {string} A prefixed unique identifier.
 */
export const unique = (prefix) =>
    `${prefix}_${++uniqueId}`;
