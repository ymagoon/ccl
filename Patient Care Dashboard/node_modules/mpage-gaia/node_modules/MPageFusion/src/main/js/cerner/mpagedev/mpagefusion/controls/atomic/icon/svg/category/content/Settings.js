"use strict";

import SvgIcon from "../../SvgIcon";
import { settings } from "../../svgDefinitions";

/**
 * The Settings icon class constructor
 * @class Settings
 * @extends SvgIcon
 */
export default class Settings extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Settings";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return settings;
    }
}
