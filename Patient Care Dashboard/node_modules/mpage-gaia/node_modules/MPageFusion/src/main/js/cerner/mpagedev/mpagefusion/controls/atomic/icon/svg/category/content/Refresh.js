"use strict";

import SvgIcon from "../../SvgIcon";
import { refresh } from "../../svgDefinitions";

/**
 * The Refresh icon class constructor
 * @class Refresh
 * @extends SvgIcon
 */
export default class Refresh extends SvgIcon {
    /**
     * @inheritDoc
     */
    get _name() {
        return "Refresh";
    }

    /**
     * @inheritDoc
     */
    getSvg() {
        return refresh;
    }
}
