"use strict";

const NAMESPACE = "mpageui-CollapseButtonGroup";

/**
 *
 * @type {
 *      {
 *          COLLAPSE_BUTTON_GROUP: {
 *              SELECTION_CHANGE: string
 *          }
 *          MENU: {
 *              SELECTION_CHANGE: string,
 *              TOGGLE: string
 *          },
 *          BUTTON_GROUP: {
 *              SELECTION_CHANGE: string
 *          }
 *      }
 * }
 */
const EVENTS = {
    COLLAPSE_BUTTON_GROUP: {
        SELECTION_CHANGE: "CollapseButtonGroup::selectionChange"
    },
    MENU: {
        SELECTION_CHANGE: "CollapseButtonGroup::MenuButton::selectionChange",
        TOGGLE: "CollapseButtonGroup::MenuButton::toggle"
    },
    BUTTON_GROUP: {
        SELECTION_CHANGE: "CollapseGroup::ButtonGroup::selectionChange"
    }
};

export {
    EVENTS,
    NAMESPACE
};
