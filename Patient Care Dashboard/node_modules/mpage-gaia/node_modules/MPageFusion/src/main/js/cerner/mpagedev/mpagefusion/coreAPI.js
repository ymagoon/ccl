import * as typeChecks from "./helpers/core/typeChecks";
import mediaQuery from "./base/MediaQuery";
import FullScreenBehavior from "./behaviors/FullScreenBehavior";
import { unique } from "./helpers/core/generators";
import {
    addWindowResizeListener,
    removeWindowResizeListener
} from "./helpers/dom/dom";

const behaviors = {
    FullScreenBehavior
};
const base = {
    mediaQuery
};
const helpers = {
    core: {
        unique
    },
    dom: {
        addWindowResizeListener,
        removeWindowResizeListener
    }
};

export {
    base,
    behaviors,
    helpers,
    typeChecks
};
