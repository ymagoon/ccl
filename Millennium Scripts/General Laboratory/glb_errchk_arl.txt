 ;;Solution_Test/script/PathNet -- Gen Lab/glb_errchk_arl/glb_errchk_arl.prg Turn on black mode

drop program GLB_ERRCHK_ARL:dba go
create program GLB_ERRCHK_ARL:dba

/* Error checking for the arl_id */
select into 'nl:'
  arl.arl_id
from
  assay_resource_lot arl
where arl.arl_id = 0
    and arl.service_resource_cd > 0
with
  nocounter

if (curqual != 0)
  set request->setup_proc[1]->success_ind = 0
  set request->setup_proc[1]->error_msg = "A row in ASSAY_RESOURCE_LOT table does not have a ARL_ID assigned."
else
  set request->setup_proc[1]->success_ind = 1
  set request->setup_proc[1]->error_msg = "The ASSAY_RESOURCE_LOT table has been updated correctly."
endif

execute dm_add_upt_setup_proc_log

end
go

;Generated by GNU enscript 1.6.4.
