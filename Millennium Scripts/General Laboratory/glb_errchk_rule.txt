 ;;Solution_Test/script/PathNet -- Gen Lab/glb_errchk_rule/glb_errchk_rule.prg Turn on black mode

drop program GLB_ERRCHK_RULE:dba go
create program GLB_ERRCHK_RULE:dba

/* Error checking for the rule */
select into 'nl:'
  qcr.max_req_results
from
  qc_rule qcr
where qcr.rule_id > 0
    and qcr.max_req_results = 0
with
  nocounter

if (curqual != 0)
  set request->setup_proc[1]->success_ind = 0
  set request->setup_proc[1]->error_msg = "A row in QC_RULE table has MAX_REQ_RESULTS = 0, review table to see if ok."
else
  set request->setup_proc[1]->success_ind = 1
  set request->setup_proc[1]->error_msg = "The QC_RULE table has been updated correctly."
endif

execute dm_add_upt_setup_proc_log

end
go

;Generated by GNU enscript 1.6.4.
